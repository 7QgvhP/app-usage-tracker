# 変更履歴

本プロジェクトは [セマンティックバージョニング](https://semver.org/lang/ja/) に従う。

**このリポジトリは v2.30.0 の時点から git 管理を開始した。** それ以前は git を使わずに
開発していたため、過去のコードは残っていない。以下のうち v2.25.0 以降は開発時の記録に
基づく詳細、それより前は README・設計メモから復元できた範囲の要約である。

---

## [2.30.0] - 2026-08-28

### 追加

- **Notion への日次登録**。1日1ページを作り、前日分の使用状況を登録する
  - 日付が変わったタイミングで自動送信し、送れなかった日は起動時に遡って補う（既定7日）
  - 同じ日を送り直すと既存ページを更新するため、重複した行はできない
  - 要約の文章は言語モデルを使わず決まった形で組み立てる。生活の記録は数値がずれては
    いけないことと、毎日同じ形であることが読み返す上でも利点になるため
  - 1日あたり約2.7KB・リクエスト1〜2回（Notion の上限は 500KB／平均3件毎秒）
- `POST /api/notion/send`（手動送信）
- スキーマ v5: `notion_sync` 表（送信済みの日とページID）

## [2.29.0] - 2026-08-27

### 修正

- **制限時間が働かない不具合。** 制限を**表示名**で保存していたため、記録される名前と
  表示名が違うアプリでは通知が出なかった（PCは `chrome.exe` と記録し、画面は `Chrome` と
  表示する。画面から `Chrome` に設定した制限をトラッカーは見つけられなかった）
- 超過数の判定も同じ理由で取りこぼしていたため、照合キーでの突き合わせに変更
- ミニウィンドウと通知の文面が記録名のままだったのを、画面と同じ表示名へ統一

### 変更

- スキーマ v4: `app_limits` を表示名から照合キー（`app_key`）へ
- 名前解決を `app/storage/display.py` へ集約し、`store.py` から命名の知識をなくした
- 用途でメソッドを分離（`get_limits()` はキー、`get_limits_by_name()` は表示名）

## [2.28.0] - 2026-08-27

### リファクタリング

- `app/storage.py`（938行）を `constants` / `dates` / `migrations` / `store` へ分割
- `app/web/api.py`（529行）を `usage` / `timeline` / `limits` / `status` / `sync` へ分割
- SQLite の例外処理20か所を `_guard` デコレータへ集約
- `templates/base.html` を新設し、3画面で重複していたヘッダーを共通化
- `_validate()` を自動化。設定を増やしても検証関数を直す必要がなくなった

外部から見た名前と応答は変えていない（API 14エンドポイントの応答が分割前と一致、
3画面の出力HTMLも一致することを確認済み）。

## [2.27.1] - 2026-08-27

### 修正

- **時間の表記が場所によって違っていた。** ミニウィンドウ（Python）だけが `0時間5分` と
  表示し、画面（JavaScript）とスマホ（Kotlin）は `5分` だった。Python 側を揃えた
- 配色が CSS と JavaScript に二重定義され、グラフの6色目が CSS に存在しない状態だった。
  CSS 変数を唯一の出所とし、JavaScript は `theme` 経由で読むようにした

### 削除

- `UsageStore.get_devices()`（本体から未使用）

### テスト基盤追加

- `tests/duration_cases.py` を唯一の仕様とし、Python / JavaScript / Kotlin の
  3実装を同じ表で検証するようにした

## [2.27.0] - 2026-08-27

### 追加

- 制限時間の一覧に、その日の使用状況と横棒を表示（残り5分で色が変わり、超過で朱色）
- 合計の横に前の期間との増減を表示。期間が途中の場合は前の期間も同じ日数だけ比較する

## [2.26.0] - 2026-08-27

### 追加

- **データベースの自動バックアップ。** 1日1回 `data/backups/` へ複製し、世代数の上限を
  超えた古いものから削除する（既定7世代）
- SQLite のバックアップAPIを使うため、動作中でも書き込み途中の状態が混ざらない

## [2.25.0] - 2026-08-27

### 削除

- `config.json` の `apps.name_map`。`data/app_names.json` の対応表と役目が重複しており、
  ブラウザ判定が対応表より先に効く問題の原因にもなっていたため廃止した

これにより設定ファイルの役割が分かれた。`config.json` は「名前をどう決めるか」、
`data/app_names.json` は「決まった名前をどう読み替えるか」。

---

## v2.24.1 以前

git 管理を開始する前のため、コミット単位の記録は残っていない。
README と設計メモから確認できる主な変更は以下のとおり。

- **v2.22.0** 同期が止まった端末をヘッダーに警告として表示（スキーマ v3）
- **v2.18.2** アプリ名の対応表（`data/app_names.json`）を導入し、PCとスマホ・PC内の
  表記ゆれを照合キーで束ねられるようにした
- **v2.17.1** 同時使用の二重計上を解消。区間の重なりを除いた実時間を出すようにした
- **v2.17.0** 受け取ったスマホのデータをPCの画面へ合算表示
- **v2.16.0** スマホからの受信基盤。スキーマ移行の仕組み、端末を区別する列、
  受信API `POST /api/sync/usage`、LAN公開時のアクセス制御（スキーマ v1・v2）
- **v2.15.0** 実行ファイルの情報（`FileDescription` / `ProductName`）から
  読みやすい表示名を得る仕組み

### Android版

`android/` 配下は独立してバージョンを持つ（現在 v1.3.0）。
実機（Nothing A069 / Android 16）で同期を確認済み。

- **v1.1.0** スマホ単体の利用状況画面
- **v1.0.0** 初版。`UsageStatsManager` による計測と、WorkManager による15分ごとの同期
