# ビルド時のメモリ割り当て
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# モジュールごとに必要な依存だけを見えるようにする
android.useAndroidX=true
kotlin.code.style=official
# 変更のあったモジュールだけを再ビルドする
org.gradle.caching=true
org.gradle.parallel=true
