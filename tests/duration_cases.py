"""
duration_cases.py - 時間の表記の共通仕様

「秒 → 表示文字列」の規則は、PC本体（Python）・画面（JavaScript）・
スマホ（Kotlin）の3つに別々に実装されている。言語が違うためコードは共有できないが、
**同じ数値が場所によって違う文字列になってはいけない**。

この表を唯一の仕様とし、3つの実装すべてを同じ内容で検証する。

- Python: tests/test_mini_window.py
- JavaScript: tests/test_web.py（ブラウザを使わず common.js を読んで検証）
- Kotlin: android/app/src/test/java/.../CommonTest.kt
"""

# (秒数, 期待する表示)
DURATION_CASES = [
    (0, "0分"),
    (1, "1分未満"),
    (30, "1分未満"),
    (59, "1分未満"),
    (60, "1分"),
    (90, "1分"),
    (45 * 60, "45分"),
    (3600, "1時間"),
    (3661, "1時間1分"),
    (5400, "1時間30分"),
    # 秒は切り捨てる（1時間59分59秒は繰り上がらない）
    (3600 + 59 * 60 + 59, "1時間59分"),
    # 24時間を超えても日へは繰り上げない
    (25 * 3600, "25時間"),
]

# (分, 期待する表示)
MINUTES_CASES = [
    (0, "0分"),
    (30, "30分"),
    (90, "1時間30分"),
    (120, "2時間"),
    (1500, "25時間"),
]
